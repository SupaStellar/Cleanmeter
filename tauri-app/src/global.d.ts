// Injected by Vite `define` (see vite.config.ts) — the package.json version,
// used as the browser-preview fallback for getAppVersion(). The packaged Tauri
// app reads the real version from Rust (CARGO_PKG_VERSION) instead.
declare const __APP_VERSION__: string;
