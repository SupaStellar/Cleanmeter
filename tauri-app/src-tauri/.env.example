# Injected at build time (CI / shell env). NOT committed with real values.
# The Rust submit_feedback command reads these via option_env! at compile time.
FEEDBACK_PORTAL_URL=https://your-portal.up.railway.app
FEEDBACK_WRITE_KEY=replace-with-the-same-key-as-the-portal
